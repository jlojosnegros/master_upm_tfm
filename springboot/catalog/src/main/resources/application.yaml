application:
  name: catalog-service

spring:
  metrics:
    prometheus:
      enabled: true
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: '*'
    jmx:
      exposure:
        include: '*'